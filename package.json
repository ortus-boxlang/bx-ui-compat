{
	"name": "bx-ui-compat-js-tests",
	"private": true,
	"description": "JavaScript unit tests for bx-ui-compat module scripts",
	"scripts": {
		"test": "vitest run",
		"test:watch": "vitest",
		"test:coverage": "vitest run --coverage"
	},
	"devDependencies": {
		"vitest": "^3.1.0",
		"jsdom": "^26.0.0",
		"@vitest/coverage-istanbul": "^3.1.0"
	}
}
